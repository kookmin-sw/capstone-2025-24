import React from 'react';

const MonitorPage: React.FC = () => {
  return (
    <div>
      <h1>MonitorPage Page</h1>
      {/* 라우터 파일에서 예시로 쓰이기 위해서 만들어 놓은 파일입니다. */}
    </div>
  );
};

export default MonitorPage;